# kube-ps1: Kubernetes logs from cluster using format "Resource, Namespace, Name, CPU, Memory"

A script that returning logs from real cluster namespace kube-system in format: "Resource, Namespace, Name, CPU, Memory"
